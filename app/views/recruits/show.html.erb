    <%= @recruit.title %>
    <%= @recruit.text %>
    <%= @recruit.place %>
    <%= link_to(@organizer.name, "/organizers/#{@organizer.id}") %>
  <% if @organizer.id == @current_organizer.id %>
    <%= link_to("編集", edit_recruit_path) %>
    <%= link_to("削除", recruit_path,{method: "DELETE"}) %>
  <%end%>
